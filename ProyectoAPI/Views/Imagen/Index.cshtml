@model IEnumerable<ProyectoAPI.Models.Imagen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.imagen1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.paso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Publicacion.titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.imagen1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.paso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Publicacion.titulo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
